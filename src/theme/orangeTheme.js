import { createTheme } from "@mui/material";

export const orangeTheme = createTheme({
  palette: {
    primary: {
      main: "#ff9505",
      contrastText: "#fff",
    },
    background: {
      main: "#F5F5F5",
    },
    error: {
      main: "#FF1E1E",
    },
    text: {
      main: "#373737",
    },
  },
});
